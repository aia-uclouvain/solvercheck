package be.uclouvain.solvercheck.assertions.stateful;

/**
 * This is really just an interface to unify the types of the operations used
 * in a stateful property validation (dive). These operations are namely:
 * - Push
 * - Pop
 * - Branching
 */
/* package */ interface DiveOperation { }
